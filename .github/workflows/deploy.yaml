name: Build and Deploy Asset Storage

on:
  push:
    branches: [main]
    paths:
      - 'VERSION'
      - 'Dockerfile'
      - 'api/**'
      - 'assets/**'
      - 'nginx.conf'
  workflow_dispatch:

env:
  REGISTRY: docker.io
  IMAGE_NAME: kingbrady/asset-storage

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Read version
        id: version
        run: echo "VERSION=$(cat VERSION)" >> $GITHUB_OUTPUT

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Asset Storage
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:${{ steps.version.outputs.VERSION }}
            ${{ env.IMAGE_NAME }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Update asset storage deployment image tag
        run: |
          sed -i "s|image: kingbrady/asset-storage:.*|image: kingbrady/asset-storage:${{ steps.version.outputs.VERSION }}|" kube/deployment.yaml

      - name: Commit deployment version updates
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add kube/deployment.yaml
          git diff --cached --quiet || git commit -m "chore: update deployment images to v${{ steps.version.outputs.VERSION }} [skip ci]"
          git push

      - name: Set up kubectl
        uses: azure/setup-kubectl@v4

      - name: Configure kubeconfig
        run: |
          mkdir -p ~/.kube
          echo "${{ secrets.KUBECONFIG }}" | base64 -d > ~/.kube/config
          chmod 600 ~/.kube/config

      - name: Deploy to Kubernetes
        run: |
          kubectl apply -f kube/deployment.yaml
          kubectl rollout status deployment/asset-storage -n asset-storage --timeout=300s

      - name: Cleanup kubeconfig
        if: always()
        run: rm -f ~/.kube/config
